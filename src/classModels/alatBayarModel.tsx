export interface AlatBayarModel {
  idperusahaan: string | null,
  idalatbayar: string | null,
  kodealatbayar: string | null,
  namaalatbayar: string | null,
  urutan: string | null,
  amount: string | null,
  akunkas: string | null,
  gambar: string | null,
  catatan: string | null,
  userentry: string | null,
  tglentry: string | null,
  status: string | null,
}