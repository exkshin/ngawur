export interface DetailBarangModel {
  idbarang: string | null,
  namabarang: string | null,
  kodebarang: string | null,
  kodemerk: string | null,
  partnumber: string | null,
  barcodesatuan1: string | null,
  barcodesatuan2: string | null,
  barcodesatuan3: string | null,
  satuan: string | null,
  konversi1: string | null,
  satuan2: string | null,
  konversi2: string | null,
  satuan3: string | null,
  isicollie: string | null,
  weight: string | null,
  ukuran: string | null,
  warna: string | null,
  hargabeli: string | null,
  hargajualmin: string | null,
  hargajualmax: string | null,
  limitmin: string | null,
  limitmax: string | null,
  gambar: string | null,
  catatan: string | null,
  tglentry: string | null,
  status: string | null,
  kodeperkiraanpersediaan: string | null,
  namaperkiraanpersediaan: string | null,
  kodeperkiraanhpp: string | null,
  namaperkiraanhpp: string | null,
  idperkiraanpersediaan: string | null,
  idperkiraanhpp: string | null,
  namamerk: string | null,
  userbuat: string | null,
  kategoribarang: string | null,
  set: string | null,
  jmlhasilset: string | null,
  scanbarcodebbk: string | null,
  detailsetubah: string | null,
  stok: string | null,
  jual: string | null,
  produksi: string | null,
  ppn: string | null,
  poin: string | null,
  daftarsupplier: DetailBarangSupplierModel[],
  daftarlokasi: DetailBarangLokasiModel[],
  hargajual: string | null,
  pathgambar: string | null,
}

export interface DetailBarangLokasiModel {
  alamat: string | null,
  catatan: string | null,
  idlokasi: string | null,
  idperkiraan: string | null,
  idperkiraansetorankasir: string | null,
  idperusahaan: string | null,
  kodelokasi: string | null,
  konversi1poin: string | null,
  kota: string | null,
  lokasidefault: string | null,
  minimaltransaksipoin: string | null,
  namalokasi: string | null,
  negara: string | null,
  propinsi: string | null,
  selisihsetoranmax: string | null,
  selisihsetoranmin: string | null,
  status: string | null,
  telp: string | null,
  tglentry: string | null,
  userentry: string | null,
}

export interface DetailBarangSupplierModel {
  alamat: string | null,
  alamatbank: string | null,
  badanusaha: string | null,
  catatan: string | null,
  contactperson: string | null,
  email: string | null,
  emailcp: string | null,
  fax: string | null,
  idperusahaan: string | null,
  idsupplier: string | null,
  idsyaratbayar: string | null,
  kodepos: string | null,
  kodesupplier: string | null,
  kodesupplierlama: string | null,
  kota: string | null,
  namabank: string | null,
  namabeneficiary: string | null,
  namasupplier: string | null,
  negara: string | null,
  negarabank: string | null,
  nomorrouting: string | null,
  norekening: string | null,
  npwp: string | null,
  propinsi: string | null,
  status: string | null,
  swiftcode: string | null,
  telp: string | null,
  telpcp: string | null,
  tglentry: string | null,
  userentry: string | null,
  website: string | null,
}