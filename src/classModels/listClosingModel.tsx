export interface ListClosingModel {
  idclosing: string | null,
  idjual: string | null,
  username: string | null,
  tanggal: string | null,
  omzet: string | null,
  setoran: string | null,
  catatan:string | null,
}