export interface DetailJualModel {
  header: HeaderDetailJual,
  detail: DataBarangDetailJual[],
  pembayaran: PembayaranDetailSO,
}

export interface HeaderDetailJual {
  idperusahaan: string | null,
  idjual: string | null,
  kodejual: string | null,
  jenistransaksi: string | null,
  idlokasi: string | null,
  idcustomer: string | null,
  namacustomeralias: string | null,
  idmarketing: string | null,
  idbbk: string | null,
  kodebbk: string | null,
  idorderjual: string | null,
  tglbbk: string | null,
  idso: string | null,
  kodeso: string | null,
  tglso: string | null,
  iddo: string | null,
  kodedo: string | null,
  tgldo: string | null,
  tgltrans: string | null,
  idsyaratbayar: string | null,
  tgljatuhtempo: string | null,
  nofakturpajak: string | null,
  total: string | null,
  discpersen: string | null,
  discrp: string | null,
  ppnrp: string | null,
  pembulatan: string | null,
  uangmuka: string | null,
  grandtotal: string | null,
  catatan: string | null,
  jmlcetak: string | null,
  userentry: string | null,
  tglentry: string | null,
  userbatal: string | null,
  tglbatal: string | null,
  alasanbatal: string | null,
  status: string | null,
  closing: string | null,
  kodelokasi: string | null,
  namalokasi: string | null,
  kodecustomer: string | null,
  namacustomer: string | null,
  alamat: string | null,
  kota: string | null,
  propinsi: string | null,
  negara: string | null,
  telp: string | null,
  userbuat: string | null,
  userhapus: string | null,
  maxnota: string | null,
  maxcredit: string | null,
  ceknotajatuhtempo: string | null,
}

export interface DataBarangDetailJual {
  idjual: string | null,
  kodejual: string | null,
  tgltrans: string | null,
  idbbk: string | null,
  kodebbk: string | null,
  idbarang: string | null,
  jml: string | null,
  jmlbonus: string | null,
  jmlbbk: string | null,
  terpenuhibbk: string | null,
  kodebarang: string | null,
  namabarang: string | null,
  satuan: string | null,
  satuanutama: string | null,
  konversi: string | null,
  idcurrency: string | null,
  simbol: string | null,
  ppn: string | null,
  harga: string | null,
  discpersen: string | null,
  disc: string | null,
  disckurs: string | null,
  subtotal: string | null,
  nilaikurs: string | null,
  hargakurs: string | null,
  subtotalkurs: string | null,
  urutan: string | null,
  pakaippn: string | null,
  ppnrp: string | null,
  ppnpersen: string | null,
  idsyaratbayar: string | null,
  namasyaratbayar: string | null,
  selisih: string | null,
  idcustomer: string | null,
  barcodesatuan1: string | null,
  partnumber: string | null,
  idlokasi: string | null,
  disccustomermin: string | null,
  disccustomermax: string | null,
  namacustomer: string | null,
  disctipecustomermin: string | null,
  disctipecustomermax: string | null,
  namatipecustomer: string | null,
  discmerkmin: string | null,
  discmerkmax: string | null,
  namamerk: string | null,
  kodemerk: string | null,
  idtransreferensi: string | null,
  kodetransreferensi: string | null,
  jenistransreferensi: string | null,
  namabarangalias: string | null,
  catatansj: string | null,
  catatan: string | null,
}

export interface PembayaranDetailSO {
  amount: string | null,
  idalatbayar: string | null,
  namaalatbayar: string | null,
}